/**
 * The product name, in one place. The launcher icon, the browser tab and every
 * line of copy must agree (audit UI-15): capacitor.config.ts and index.html
 * carry the same string.
 */
export const APP_NAME = 'Money Manager';
