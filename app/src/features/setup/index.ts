// First run: the welcome dialog and sample banner while the demo household is
// showing. The guided setup itself lives in features/onboarding.
export { WelcomeDialog } from './WelcomeDialog';
export { SampleBanner } from './SampleBanner';
export { DEFAULT_CATEGORIES, currencyOptions } from './defaults';
