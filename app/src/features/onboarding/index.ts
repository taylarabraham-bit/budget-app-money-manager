// Guided setup: new household (household, people, paydays, sharing, bills,
// budgets, first goal, preferences) or join the partner's household from the
// home server. Everything is applied on Finish. Mounted by App in place of
// the shell; the welcome dialog and sample banner live in features/setup.
export { OnboardingScreen } from './OnboardingScreen';
export type { Draft } from './types';
